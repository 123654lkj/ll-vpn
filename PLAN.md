# LL VPN 开发计划（PLAN.md）

> 基于 DESIGN.md V3 架构设计文档的可执行开发计划
> 生成时间：2026-06-03

---

## 目录

1. [总体概述](#总体概述)
2. [Phase 0：路由 Shim 层（P0 优先级）](#phase-0路由-shim-层)
3. [Phase 1：Mesh 核心基础](#phase-1mesh-核心基础)
4. [Phase 2：路由 Shim 与命令适配](#phase-2路由-shim-与命令适配)
5. [Phase 3：网状组网](#phase-3网状组网)
6. [Phase 4：分块存储](#phase-4分块存储)
7. [Phase 5：增量备份](#phase-5增量备份)
8. [Phase 6：高级功能](#phase-6高级功能)
9. [技术风险矩阵](#技术风险矩阵)
10. [关键技术决策点](#关键技术决策点)
11. [依赖关系图](#依赖关系图)
12. [里程碑时间线](#里程碑时间线)

---

## 总体概述

### 项目目标

将去中心化 VPN 功能整合到现有的 Lan-Link (LL) 局域网管理工具中，实现：
- 用宝可梦名字作为节点地址（替代 IP）
- Mesh 网状网络（节点互相为跳板）
- 去中心化名字注册中心（自动选举）
- 分块存储（多源并行下载）
- Noise Protocol 加密通信

### Phase 划分对照

| Phase | 设计文档阶段 | 核心交付物 | 依赖 |
|-------|-------------|-----------|------|
| P0-pre | 节点 ID 生成 | NodeID 类型、Ed25519 密钥对 | 无 | ✅ 已完成 |
|| P0-1 | 路由接口 | Router trait、路由表结构 | 无 | ✅ 已完成 |
|| P0-2 | 地址解析层 | AddressResolver trait、地址缓存 | P0-1 | ✅ 已完成 |
|| P0-3 | LAN 路由适配 | LanRouter（UDP 9876）、节点发现 | P0-1 | ✅ 已完成 |
|| P0-4 | 集成测试 | 74 个测试通过 | P0-1, P0-2, P0-3 | ✅ 已完成 |
||| P1 | Mesh 核心 | Noise 握手、TCP 中继、会话管理 | P0-pre | ✅ 已完成 |
|| P2 | 命令适配 | `ll cmd node:xxx` 可用 | P0, P1 | ✅ 已完成 |
|| P3 | 网状组网 | 中继转发、多跳路由、DHT | P1 | ✅ 已完成 |
|| P4 | 分块存储 | 块切割、加密、多源下载 | P2 | ✅ 已完成 |
| P5 | 增量备份 | 差异对比、版本历史 | P4 |
| P6 | 高级功能 | 打洞、多路径、流量统计 | P3 |

---

## Phase 0：路由 Shim 层

> **优先级**：最高（设计文档明确 P0）
> **目标**：在现有 LL 代码中建立路由抽象层，隔离网络层差异
> **前置**：P0-pre（节点 ID 生成）已完成

### 任务 P0-1：定义路由接口

> **状态**：✅ 已完成
> **完成时间**：2026-06-03
> **产出文件**：`src/router.rs`

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 现有 `ll.rs` 源码 |
| **输出** | `router.go`，定义 `Router` 接口 |
| **依赖** | 无 |
| **预计工时** | 0.5 天 |

**具体任务**：
- 定义 `Router` 接口：`Send(target string, data []byte) error`
- 定义 `RouterStatus` 结构：节点状态、连接类型（LAN/VPN）
- 定义路由表结构 `RouteTable`：目标 → 路由器映射

**验收标准**：
```rust
trait Router {
    fn send(&self, target: &str, data: &[u8]) -> Result<(), Error>;
    fn status(&self) -> RouterStatus;
    fn name(&self) -> &str;
}
```

---

> **状态**：✅ 已完成
> **完成时间**：2026-06-03
> **产出文件**：`src/address.rs`

### 任务 P0-2：实现地址解析层

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P0-1 的 `Router` 接口 |
| **输出** | `address.rs`，地址解析逻辑 |
| **依赖** | P0-1 |
| **预计工时** | 1 天 |

**具体任务**：
- 解析 `node:<名字>` 格式地址
- 解析 `node:<名字>:<端口>` 格式地址
- 实现 `AddressResolver` 接口：名字 → 节点 ID → 坐标
- 支持地址缓存（TTL 5 分钟）
- 错误处理：未知节点、解析超时

**验收标准**：
```rust
trait AddressResolver {
    fn resolve(&self, name: &str) -> Result<NodeID, Error>;
    fn cache(&self, name: &str, id: NodeID, ttl: Duration);
}
```

---

> **状态**：✅ 已完成
> **完成时间**：2026-06-03
> **产出文件**：`src/lan_router.rs`

### 任务 P0-3：适配现有 LAN 路由

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 现有 `lan.rs` 源码，任务 P0-1 |
| **输出** | `lan_router.rs`，LAN 路由器实现 |
| **依赖** | P0-1 |
| **预计工时** | 1 天 |

**具体任务**：
- 将现有 LAN 发现逻辑包装为 `Router` 实现
- 实现 `LanRouter`：UDP 9876 端口通信
- 实现节点发现：mDNS / UDP 广播
- 测试：`ll cmd node:Pikachu "uptime"`（局域网内）

**验收标准**：
- LAN 内节点发现正常
- 现有 `ll` 命令通过 Router 路由后功能不变

---

> **状态**：✅ 已完成
> **完成时间**：2026-06-03
> **产出文件**：`tests/integration_test.rs`

### 任务 P0-4：路由 Shim 集成测试

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | P0-1, P0-2, P0-3 的所有产出 |
| **输出** | 测试用例 + 集成验证 |
| **依赖** | P0-1, P0-2, P0-3 |
| **预计工时** | 0.5 天 |

**具体任务**：
- 编写单元测试：地址解析、路由分发
- 集成测试：LAN 内命令执行
- 验证 `ll ping`, `ll cmd`, `ll file` 功能正常

---

## Phase 1：Mesh 核心基础

> **目标**：建立节点间可靠加密通信的基础能力
> **核心交付**：两个节点能通过 Noise 协议可靠通信

### 任务 P1-1：节点 ID 生成

> **状态**：✅ 已完成（作为 P0-pre 实现）
> **完成时间**：2026-06-03
> **产出文件**：`src/vpn/identity.rs`

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 节点公钥 |
| **输出** | `vpn/identity.rs`，节点 ID 生成逻辑 |
| **依赖** | 无 |
| **预计工时** | 0.5 天 |

**具体任务**：
- 基于 Ed25519 公钥生成 32 字节节点 ID
- 实现 `NodeID` 类型（不可变，值类型）
- 实现 `NodeID.String()`（便于调试）
- 实现 `NodeID.Distance(other)` 用于 Yggdrasil 路由
- 持久化：首次生成后保存到本地

**验收标准**：
```rust
struct NodeID([u8; 32]);

impl NodeID {
    fn from_public_key(key: &ed25519::PublicKey) -> Self;
    fn distance(&self, other: &NodeID) -> f64;
    fn to_hex(&self) -> String;
}
```

---

### 任务 P1-2：Noise Protocol 握手实现

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P1-1 的 NodeID |
| **输出** | `vpn/handshake.rs`，Noise IK 握手 |
| **依赖** | P1-1 |
| **预计工时** | 7-10 天 |

**具体任务**：
- 实现 Noise IK 握手模式（设计文档指定）
  - A → B: Hello（临时公钥）
  - B → A: HelloACK（临时公钥 + 加密载荷）
  - A → B: Data（加密数据）
- 实现前向保密（每会话新密钥）
- 实现 Timestamp 防重放
- 实现 Nonce 防篡改
- 密钥派生：HKDF
- 密码学库选型（见关键技术决策点）

**验收标准**：
- 两个测试节点能完成握手
- 握手后能发送加密数据
- 重放攻击被拒绝

---

### 任务 P1-3：加密会话管理

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P1-2 的握手实现 |
| **输出** | `vpn/session.rs`，会话生命周期管理 |
| **依赖** | P1-2 |
| **预计工时** | 2 天 |

**具体任务**：
- 实现会话状态机：`INIT → HANDSHAKE → ESTABLISHED → CLOSING → CLOSED`
- 会话超时处理（默认 30 分钟无活动）
- 会话复用（同一节点对不重复握手）
- 会话密钥轮换（每 24 小时）
- 内存安全：敏感数据擦除

**验收标准**：
- 会话状态正确流转
- 超时自动关闭
- 密钥轮换后旧数据仍可解密（前向保密）

---

### 任务 P1-4：TCP 中继通信

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P1-2, P1-3 |
| **输出** | `vpn/relay.rs`，中继通信基础 |
| **依赖** | P1-2, P1-3 |
| **预计工时** | 2-3 天 |

**具体任务**：
- 实现 TCP 服务端：监听指定端口（默认 9876）
- 实现 TCP 客户端：连接其他节点
- 消息帧协议：`[长度][类型][Payload]`
- 消息类型：`HANDSHAKE`, `DATA`, `ACK`, `HEARTBEAT`, `RELAY`
- 实现基础中继：A 通过 B 转发数据到 C
- 连接池管理：复用 TCP 连接

**消息帧格式**：
```
+--------+--------+------------------+
| 4 bytes| 1 byte | variable length  |
| length | type   | payload          |
+--------+--------+------------------+
```

**验收标准**：
- 两个节点可通过 TCP 直连通信
- 中继转发功能正常
- 连接断开后自动重连

---

### 任务 P1-5：心跳与保活

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 任务 P1-3, P1-4 |
| **输出** | `vpn/heartbeat.rs`，心跳机制 |
| **依赖** | P1-3, P1-4 |
| **预计工时** | 1 天 |

**具体任务**：
- 实现心跳发送（默认 5 秒间隔）
- 实现心跳响应
- 节点存活检测（3 次无响应判定离线）
- 事件回调：`OnNodeOnline`, `OnNodeOffline`

**验收标准**：
- 节点离线后能被检测到
- 心跳日志可配置

---

### 任务 P1-6：入口节点引导

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P1-4, P1-5 |
| **输出** | `vpn/bootstrap.rs`，节点发现引导 |
| **依赖** | P1-4, P1-5 |
| **预计工时** | 1-2 天 |

**具体任务**：
- 实现硬编码入口节点列表（配置文件）
- 实现引导握手：新节点连接入口节点获取已知节点列表
- 实现节点列表广播
- 多入口节点冗余（至少 2 个）

**配置示例**：
```yaml
bootstrap:
  nodes:
    - host: "mewtwo.example.com"
      port: 9876
    - host: "charizard.lan"
      port: 9876
```

**验收标准**：
- 新节点能通过入口节点发现其他节点
- 入口节点不可达时有降级逻辑

---

## Phase 2：路由 Shim 与命令适配

> **目标**：让 `ll cmd node:xxx` 等命令通过 VPN 路由

### 任务 P2-1：VPN 路由器实现

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | Phase 0 的 Router 接口，Phase 1 的 VPN 核心 |
| **输出** | `vpn/vpn_router.rs`，VPN 路由器 |
| **依赖** | P0-1, P1-4, P1-6 |
| **预计工时** | 2 天 |

**具体任务**：
- 实现 `VpnRouter`：`Router` 接口的 VPN 实现
- 地址解析：名字 → NodeID → 坐标
- 路由决策：LAN 直连优先，不行走 VPN
- 实现 `Send()`：通过 Mesh 网络发送数据
- 实现 `Receive()`：接收其他节点发来的数据

---

### 任务 P2-2：命令层适配

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 任务 P2-1，现有 `ll.rs` 命令处理 |
| **输出** | 修改 `ll.rs`，集成路由 Shim |
| **依赖** | P2-1 |
| **预计工时** | 1 天 |

**具体任务**：
- 修改命令入口：解析 `node:xxx` 前缀
- 调用 `AddressResolver` 获取目标节点
- 调用 `Router.Send()` 发送命令
- 实现命令响应接收与展示
- 测试：`ll cmd node:Charizard "uptime"`

---

### 任务 P2-3：VPN 状态命令

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 任务 P2-1 |
| **输出** | `ll nodes`, `ll status` 命令 |
| **依赖** | P2-1 |
| **预计工时** | 0.5 天 |

**具体任务**：
- `ll nodes`：列出所有已知节点及状态
- `ll status`：显示当前节点信息、连接状态
- `ll ping node:xxx`：测试 VPN 连通性

---

## Phase 3：网状组网

> **目标**：多节点自动组网，中继转发，多跳路由
> **核心交付**：节点自动发现、路由、自愈

### 任务 P3-1：名字注册中心

> **状态**：✅ 已完成（代码 + 63 测试）
> **完成时间**：2026-06-03
> **产出文件**：`registry/center.rs`

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P1-4, P1-5 |
| **输出** | `registry/center.rs`，注册中心 |
| **依赖** | P1-4, P1-5 |
| **预计工时** | 3-4 天 |

**具体任务**：
- 实现注册中心服务端：
  - 接受名字注册请求
  - 维护名字 → NodeID 映射表
  - 响应名字查询
- 实现注册中心客户端：
  - 注册自身名字
  - 查询其他节点名字
  - 心跳上报
- 映射表持久化（本地文件）
- 映射表同步（定期全量同步）
- 并发安全（多节点同时注册）

**数据结构**：
```go
type NameRegistry struct {
    mu       sync.RWMutex
    entries  map[string]NodeID  // 名字 → 节点ID
    reverse  map[NodeID]string  // 节点ID → 名字
    filepath string
}
```

**验收标准**：
- 节点能注册名字
- 其他节点能查询名字
- 注册中心重启后映射表恢复

---

### 任务 P3-2：选举机制

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P3-1，任务 P1-5（心跳） |
| **输出** | `registry/election.rs`，选举逻辑 |
| **依赖** | P3-1, P1-5 |
| **预计工时** | 5-7 天 |

**具体任务**：
- 实现注册中心下线检测：
  - 连续 3 次心跳无响应 → 判定下线
  - 增加任期号（term number）防止脑裂
- 实现选举发起：
  - 发现下线的节点广播 "选举请求"
  - 所有节点参与投票
  - 处理网络分区：两个节点同时宣布对方离线时，通过 term number 解决
- 实现投票逻辑：
  - 基于节点 ID hash 的随机算法
  - 多数节点同意 → 当选
- 实现新注册中心接管：
  - 广播 "新注册中心"
  - 其他节点更新配置
- 实现旧注册中心回归处理：
  - A 恢复上线 → 发现 B 在运行
  - A 变成普通节点
  - 映射表同步到 B

**脑裂处理**：
- 两个节点同时宣布对方离线 → 比较 term number
- term number 高的获胜
- term number 相同 → 等待超时后重新选举

**状态机**：
```
NORMAL → REGISTRY_DOWN → ELECTION → NEW_REGISTRY → NORMAL
                                       ↑
A 回归 → SYNC → NORMAL ←─────────────┘
```

**验收标准**：
- 注册中心下线后能自动选举新注册中心
- 旧注册中心回来后能正确降级
- 选举过程中服务不中断（短暂降级可接受）
- 网络分区时不会出现两个注册中心同时运行
- term number 正确同步

---

### 任务 P3-3：DHT 节点发现

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P1-4, P1-6（引导） |
| **输出** | `vpn/dht.rs`，分布式哈希表 |
| **依赖** | P1-4, P1-6 |
| **预计工时** | 4-5 天 |

**具体任务**：
- 实现 Kademlia DHT（或简化版）：
  - 节点 ID 空间：256 位
  - K-桶结构：每桶 20 个节点
  - FIND_NODE / FIND_VALUE 操作
- 实现节点路由表维护：
  - 定期 ping 已知节点
  - 淘汰不响应节点
- 实现值存储（可选，用于小数据）：
  - PUT / GET 操作
  - 过期时间
- 与引导节点集成：
  - 新节点通过引导节点加入 DHT
  - 自动分裂/合并

**验收标准**：
- 新节点能通过 DHT 发现其他节点
- 节点加入/退出后 DHT 自动收敛
- 查找延迟 < 3 跳

---

### 任务 P3-4：多跳路由

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P1-4, P3-3 |
| **输出** | `vpn/multihop.rs`，多跳路由逻辑 |
| **依赖** | P1-4, P3-3 |
| **预计工时** | 2-3 天 |

**具体任务**：
- 实现路由表：目标 → 下一跳映射
- 实现路由发现：通过 DHT 获取路径
- 实现路由缓存：避免重复查找
- 实现路由失效处理：
  - 检测路径失效
  - 自动切换备用路径
- 实现 TTL：防止路由环路（最大跳数 10）

**验收标准**：
- 3 跳以内的路由能自动建立
- 路径失效后自动切换
- 路由表内存占用可控

---

### 任务 P3-5：网络自愈

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P3-4, P1-5 |
| **输出** | `vpn/selfheal.rs`，自愈逻辑 |
| **依赖** | P3-4, P1-5 |
| **预计工时** | 2 天 |

**具体任务**：
- 实现节点离线处理：
  - 检测节点离线
  - 从路由表移除
  - 通知上层应用
- 实现路径失效处理：
  - 检测路径不可达
  - 触发路由重新发现
  - 无感知切换
- 实现网络分区处理：
  - 各分区独立工作
  - 恢复后自动合并
- 实现状态恢复：
  - 重启后从持久化恢复路由表
  - 重新建立连接

---

## Phase 4：分块存储

> **目标**：实现文件分块、加密存储、多源并行下载

### 任务 P4-1：块切割逻辑

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 文件内容 |
| **输出** | `storage/chunk.rs`，块切割 |
| **依赖** | 无（可并行开发） |
| **预计工时** | 1-2 天 |

**具体任务**：
- 实现文件分块：
  - 默认块大小：4MB
  - 最后一块可能小于 4MB
- 实现块 hash 计算：SHA-256
- 实现块元数据生成：
  ```json
  {
    "file": "video.mp4",
    "size": 13651488,
    "blocks": [
      {"hash": "abc123...", "size": 4194304},
      {"hash": "def456...", "size": 4194304}
    ]
  }
  ```
- 实现块拼装：从块恢复文件
- 实现块验证：hash 校验

**验收标准**：
- 13MB 文件正确切割为 4 块
- 块 hash 唯一且可验证
- 块拼装后文件完整

---

### 任务 P4-2：加密存储

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P4-1 |
| **输出** | `storage/encrypt.rs`，加密逻辑 |
| **依赖** | P4-1 |
| **预计工时** | 1-2 天 |

**具体任务**：
- 实现 AES-256-GCM 加密：
  - 用户密钥派生（PBKDF2 或 Argon2）
  - 每块独立加密（nonce 不同）
  - 加密后块 = nonce + ciphertext + tag
- 实现解密：
  - 验证 tag
  - 解密内容
- 实现密钥管理：
  - 用户密码 → 密钥派生
  - 密钥缓存（内存中，不持久化）
- 安全要求：
  - 备份节点只存密文
  - 无法解密任何内容

**验收标准**：
- 加密/解密往返正确
- 无密钥无法解密
- 不同块使用不同 nonce

---

### 任务 P4-3：元数据管理

> **状态**：✅ 已完成（26 测试）
> **完成时间**：2026-06-03
> **产出文件**：`storage/metadata.rs`

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P4-1, P4-2 |
| **输出** | `storage/metadata.rs`，元数据管理 |
| **依赖** | P4-1, P4-2 |
| **预计工时** | 2 天 |

**具体任务**：
- 实现元数据清单：
  - 文件名、大小、块数
  - 每个块的 hash、大小、存储节点列表
  - 元数据本身也加密存储
- 实现元数据存储：
  - 存储到注册中心或指定节点
  - 支持多副本
- 实现元数据查询：
  - 通过文件名查询元数据
  - 获取块分布信息
- 实现元数据同步：
  - 节点间元数据同步
  - 冲突解决（最新优先）

---

### 任务 P4-4：多源并行下载

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P4-1, P4-2, P4-3, P1-4 |
| **输出** | `storage/download.rs`，并行下载 |
| **依赖** | P4-1, P4-2, P4-3, P1-4 |
| **预计工时** | 3-4 天 |

**具体任务**：
- 实现块请求协议：
  - 请求指定块 hash
  - 响应块数据
- 实现多源下载：
  - 并行向多个节点请求不同块
  - 块级别并发（不是文件级别）
  - 进度报告
- 实现重排序缓冲区：
  - 按 seq 顺序输出
  - 处理乱序到达
- 实现断点续传：
  - 记录已下载块
  - 从断点继续
- 实现下载调度：
  - 优先下载稀有块
  - 负载均衡

**验收标准**：
- 同时从多个节点下载，带宽叠加
- 部分节点不可达时自动切换
- 下载中断后能续传

---

### 任务 P4-5：备份命令集成

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | P4-1 到 P4-4，P2-1 |
| **输出** | `ll backup` 和 `ll restore` 命令 |
| **依赖** | P4-1, P4-2, P4-3, P4-4, P2-1 |
| **预计工时** | 2 天 |

**具体任务**：
- `ll backup /path node:Charizard`：
  - 分块 → 加密 → 上传到指定节点
- `ll restore node:Charizard:/path`：
  - 查询元数据 → 并行下载 → 解密 → 拼装
- `ll file put node:Charizard:/path`：
  - 单文件传输
- 进度显示和错误处理

---

## Phase 5：增量备份

> **目标**：只传变化部分，支持版本历史和时间点恢复

### 任务 P5-1：块差异对比

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P4-1 |
| **输出** | `storage/diff.rs`，差异检测 |
| **依赖** | P4-1 |
| **预计工时** | 2 天 |

**具体任务**：
- 实现块级差异检测：
  - 对比本地文件与远程元数据
  - 找出新增/修改/删除的块
- 实现文件级差异：
  - 对比文件 hash
  - 只传输变化文件
- 实现差异报告：
  - 显示待传输块数
  - 预估传输时间

---

### 任务 P5-2：增量同步

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P5-1, P4-4 |
| **输出** | `storage/incremental.rs`，增量同步 |
| **依赖** | P5-1, P4-4 |
| **预计工时** | 2-3 天 |

**具体任务**：
- 实现增量上传：
  - 只上传变化的块
  - 更新元数据清单
- 实现增量下载：
  - 只下载缺失的块
  - 合并到本地
- 实现同步状态跟踪：
  - 记录最后同步时间
  - 快速判断是否需要同步

---

### 任务 P5-3：版本历史

| 属性 | 值 |
|------|-----|
| **复杂度** | 中等 |
| **输入** | 任务 P5-2 |
| **输出** | `storage/version.go`，版本管理 |
| **依赖** | P5-2 |
| **预计工时** | 2 天 |

**具体任务**：
- 实现版本标记：
  - 每次备份创建版本快照
  - 版本号：时间戳 + 序号
- 实现版本列表：
  - `ll versions node:Charizard:/data`
- 实现时间点恢复：
  - `ll restore --version 20260601 node:Charizard:/data`

---

### 任务 P5-4：存储清理

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 任务 P5-3 |
| **输出** | `storage/gc.go`，垃圾回收 |
| **依赖** | P5-3 |
| **预计工时** | 1 天 |

**具体任务**：
- 实现引用计数：
  - 跟踪每个块被多少版本引用
- 实现过期清理：
  - 删除无引用的块
  - 删除过期版本
- 实现存储统计：
  - 总存储量、块数、版本数

---

## Phase 6：高级功能

> **目标**：性能优化和高级网络功能

### 任务 P6-1：UDP 打洞

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P1-4 |
| **输出** | `vpn/hole_punch.rs`，NAT 穿透 |
| **依赖** | P1-4 |
| **预计工时** | 3-4 天 |

**具体任务**：
- 实现 UDP 打洞：
  - 双方同时发 UDP 包
  - NAT 设备建立映射
  - 之后直接通信
- 实现打洞失败处理：
  - 找公网节点中继
- 实现对称 NAT 检测：
  - 只能走中继
- 与中继机制集成：
  - 打洞失败自动降级到中继

**技术风险**：
- 对称 NAT 无法穿透（只能中继）
- 不同 NAT 类型行为差异
- 打洞超时处理

---

### 任务 P6-2：多路径并行传输

| 属性 | 值 |
|------|-----|
| **复杂度** | **复杂** |
| **输入** | 任务 P3-4, P1-4 |
| **输出** | `vpn/multipath.rs`，多路径传输 |
| **依赖** | P3-4, P1-4 |
| **预计工时** | 4-5 天 |

**具体任务**：
- 实现数据分片：
  - 大数据块拆分为小片段
  - 每个片段带序列号
- 实现多路径发送：
  - 同时通过多条路径发送
  - 路径评分：`延迟 × 0.7 + 跳数 × 100 × 0.3`
- 实现重排序缓冲区：
  - 按 seq 顺序输出
  - 处理乱序和丢失
- 实现 ACK 机制：
  - 每条路径独立 ACK
  - 超时重传
- 实现路径质量监控：
  - 实时评估路径质量
  - 动态调整路径权重

**验收标准**：
- 多路径带宽 > 单路径
- 路径失效自动切换
- 数据完整性保证

---

### 任务 P6-3：流量统计与限制

| 属性 | 值 |
|------|-----|
| **复杂度** | 简单 |
| **输入** | 任务 P1-4 |
| **输出** | `vpn/stats.rs`，流量统计 |
| **依赖** | P1-4 |
| **预计工时** | 1 天 |

**具体任务**：
- 实现流量计数：
  - 上行/下行字节数
  - 中继流量统计
- 实现速率限制：
  - 中继流量限速
  - 优先级：自己的流量 > 密钥环内 > 其他
- 实现统计展示：
  - `ll stats` 命令

---

## 技术风险矩阵

### 高风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| **Noise Protocol 实现错误** | 安全漏洞 | 中 | 使用成熟库（如 `go-noise`），严格测试 |
| **NAT 穿透失败** | 部分节点无法直连 | 高 | 中继机制作为降级方案 |
| **选举机制脑裂** | 多个注册中心 | 中 | 多数投票 + 任期号 |
| **分块存储数据损坏** | 文件不可恢复 | 低 | 冗余存储 + hash 校验 |
| **Yggdrasil 算法适配** | 路由不正确 | 中 | 参考官方实现，充分测试 |

### 中风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| **DHT 收敛慢** | 节点发现延迟 | 中 | 引导节点 + 缓存 |
| **内存占用过高** | 低端设备不可用 | 中 | 限制路由表大小 |
| **并发安全** | 数据竞争 | 中 | 读写锁 + 原子操作 |
| **密钥管理** | 密钥泄露 | 低 | 内存擦除 + 最小权限 |

### 低风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| **配置文件格式变更** | 兼容性问题 | 低 | 版本化配置 |
| **日志过多** | 磁盘占满 | 低 | 日志轮转 |

---

## 关键技术决策点

### 决策 1：密码学库选型

**选项**：
1. `go-noise`（纯 Go 实现）
2. `crypto/noise`（官方库）
3. 自己实现（不推荐）

**建议**：使用成熟的 Go Noise 库，如 `github.com/flynn/noise`

**决策时间**：Phase 1 开始前

---

### 决策 2：DHT 实现方式

**选项**：
1. 完整 Kademlia 实现
2. 简化版 DHT（小规模网络）
3. 集中式发现 + 分布式缓存

**建议**：先实现简化版 DHT，根据实际节点规模决定是否升级

**决策时间**：Phase 3 开始前

---

### 决策 3：块大小选择

**选项**：
1. 4MB（默认）
2. 1MB（更灵活）
3. 自适应（根据网络条件）

**建议**：默认 4MB，支持配置修改

**决策时间**：Phase 4 开始前

---

### 决策 4：元数据存储位置

**选项**：
1. 存储在注册中心
2. 存储在备份节点
3. 分布式存储（多副本）

**建议**：先存在注册中心，后续支持多副本

**决策时间**：Phase 4 开始前

---

## 依赖关系图

```
Phase 0: 路由 Shim
  P0-1 (路由接口)
  P0-2 (地址解析) ← P0-1
  P0-3 (LAN 适配) ← P0-1
  P0-4 (集成测试) ← P0-1, P0-2, P0-3
      ↓
Phase 1: Mesh 核心
  P1-1 (节点 ID)        ← 无依赖
  P1-2 (Noise 握手)     ← P1-1
  P1-3 (会话管理)       ← P1-2
  P1-4 (TCP 中继)       ← P1-2, P1-3
  P1-5 (心跳保活)       ← P1-3, P1-4
  P1-6 (入口引导)       ← P1-4, P1-5
      ↓
Phase 2: 命令适配
  P2-1 (VPN 路由器)     ← P0-1, P1-4, P1-6
  P2-2 (命令适配)       ← P2-1
  P2-3 (状态命令)       ← P2-1
      ↓
Phase 3: 网状组网
  P3-1 (注册中心)       ← P1-4, P1-5
  P3-2 (选举机制)       ← P3-1, P1-5
  P3-3 (DHT)            ← P1-4, P1-6
  P3-4 (多跳路由)       ← P1-4, P3-3
  P3-5 (网络自愈)       ← P3-4, P1-5
      ↓
Phase 4: 分块存储
  P4-1 (块切割)         ← 无依赖（可并行）
  P4-2 (加密存储)       ← P4-1
  P4-3 (元数据管理)     ← P4-1, P4-2
  P4-4 (并行下载)       ← P4-1, P4-2, P4-3, P1-4
  P4-5 (命令集成)       ← P4-1~P4-4, P2-1
      ↓
Phase 5: 增量备份
  P5-1 (差异对比)       ← P4-1
  P5-2 (增量同步)       ← P5-1, P4-4
  P5-3 (版本历史)       ← P5-2
  P5-4 (存储清理)       ← P5-3
      ↓
Phase 6: 高级功能
  P6-1 (UDP 打洞)       ← P1-4
  P6-2 (多路径)         ← P3-4, P1-4
  P6-3 (流量统计)       ← P1-4
```

---

## 里程碑时间线

### 里程碑 M1：基础通信（Phase 0 + Phase 1）

**目标**：两个节点能通过 VPN 加密通信

| 任务 | 工时 | 累计 |
|------|------|------|
| P0-1 | 0.5 天 | 0.5 天 |
| P0-2 | 1 天 | 1.5 天 |
| P0-3 | 1 天 | 2.5 天 |
| P0-4 | 0.5 天 | 3 天 |
| P1-1 | 0.5 天 | 3.5 天 |
| P1-2 | 3-5 天 | 6.5-8.5 天 |
| P1-3 | 2 天 | 8.5-10.5 天 |
| P1-4 | 2-3 天 | 10.5-13.5 天 |
| P1-5 | 1 天 | 11.5-14.5 天 |
| P1-6 | 1-2 天 | 12.5-16.5 天 |

**预计时间**：2-3 周

---

### 里程碑 M2：命令可用（Phase 2）

**目标**：`ll cmd node:xxx` 通过 VPN 可用

| 任务 | 工时 | 累计 |
|------|------|------|
| P2-1 | 2 天 | 2 天 |
| P2-2 | 1 天 | 3 天 |
| P2-3 | 0.5 天 | 3.5 天 |

**预计时间**：1 周

---

### 里程碑 M3：自动组网（Phase 3）

**目标**：多节点自动发现、路由、自愈

| 任务 | 工时 | 累计 |
|------|------|------|
| P3-1 | 3-4 天 | 3-4 天 |
| P3-2 | 3-4 天 | 6-8 天 |
| P3-3 | 4-5 天 | 10-13 天 |
| P3-4 | 2-3 天 | 12-16 天 |
| P3-5 | 2 天 | 14-18 天 |

**预计时间**：3-4 周

---

### 里程碑 M4：分块存储（Phase 4）

**目标**：文件备份和恢复可用

| 任务 | 工时 | 累计 |
|------|------|------|
| P4-1 | 1-2 天 | 1-2 天 |
| P4-2 | 1-2 天 | 2-4 天 |
| P4-3 | 2 天 | 4-6 天 |
| P4-4 | 3-4 天 | 7-10 天 |
| P4-5 | 2 天 | 9-12 天 |

**预计时间**：2-3 周

---

### 里程碑 M5：增量备份（Phase 5）

**目标**：增量同步和版本管理

| 任务 | 工时 | 累计 |
|------|------|------|
| P5-1 | 2 天 | 2 天 |
| P5-2 | 2-3 天 | 4-5 天 |
| P5-3 | 2 天 | 6-7 天 |
| P5-4 | 1 天 | 7-8 天 |

**预计时间**：1.5-2 周

---

### 里程碑 M6：高级功能（Phase 6）

**目标**：性能优化和高级网络功能

| 任务 | 工时 | 累计 |
|------|------|------|
| P6-1 | 3-4 天 | 3-4 天 |
| P6-2 | 4-5 天 | 7-9 天 |
| P6-3 | 1 天 | 8-10 天 |

**预计时间**：2 周

---

## 总工时估算

| Phase | 工时范围 | 优先级 |
|-------|---------|--------|
| Phase 0 | 3 天 | P0 |
| Phase 1 | 9.5-13.5 天 | P1 |
| Phase 2 | 3.5 天 | P2 |
| Phase 3 | 14-18 天 | P3 |
| Phase 4 | 9-12 天 | P4 |
| Phase 5 | 7-8 天 | P5 |
| Phase 6 | 8-10 天 | P6 |
| **总计** | **54-75 天** | - |

**建议**：
- 优先完成 M1-M3（基础通信 + 命令 + 组网）
- M4-M5 可与 M3 并行开发
- M6 可延后，等基础稳定后再做

---

## 附录：文件结构映射

```
lan-link/
├── ll.go                    # 主入口（修改：集成路由 Shim）
├── lan.go                   # 局域网模块（修改：适配 Router 接口）
├── router.go                # 路由 Shim（新增：P0-1）
├── address.go               # 地址解析（新增：P0-2）
├── lan_router.go            # LAN 路由器（新增：P0-3）
├── vpn/
│   ├── identity.go          # 节点 ID（新增：P1-1）
│   ├── handshake.go         # Noise 握手（新增：P1-2）
│   ├── session.go           # 会话管理（新增：P1-3）
│   ├── relay.go             # 中继通信（新增：P1-4）
│   ├── heartbeat.go         # 心跳保活（新增：P1-5）
│   ├── bootstrap.go         # 入口引导（新增：P1-6）
│   ├── vpn_router.go        # VPN 路由器（新增：P2-1）
│   ├── dht.go               # 路由发现（新增：P3-3）
│   ├── multihop.go          # 多跳路由（新增：P3-4）
│   ├── selfheal.go          # 网络自愈（新增：P3-5）
│   ├── hole_punch.go        # 打洞优化（新增：P6-1）
│   ├── multipath.go         # 多路径（新增：P6-2）
│   └── stats.go             # 流量统计（新增：P6-3）
├── storage/
│   ├── chunk.go             # 分块逻辑（新增：P4-1）
│   ├── encrypt.go           # 加密逻辑（新增：P4-2）
│   ├── metadata.go          # 元数据管理（新增：P4-3）
│   ├── download.go          # 并行下载（新增：P4-4）
│   ├── diff.go              # 差异检测（新增：P5-1）
│   ├── incremental.go       # 增量同步（新增：P5-2）
│   ├── version.go           # 版本管理（新增：P5-3）
│   └── gc.go                # 垃圾回收（新增：P5-4）
├── registry/
│   ├── center.go            # 注册中心（新增：P3-1）
│   └── election.go          # 选举逻辑（新增：P3-2）
├── config.yaml              # 配置文件
└── docs/
    └── vpn.md               # VPN 文档
```

---

> **文档版本**：v1.0
> **最后更新**：2026-06-03
